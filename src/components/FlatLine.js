import React from "react";
import { View } from "react-native";

export default () => {
  return (
    <View
      style={{
        width: "100%",
        borderBottomColor: "#BCBCBC",
        borderBottomWidth: 1,
        marginTop: 5,
      }}
    ></View>
  );
};
